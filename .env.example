# ── Backend ──
PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# ── Auth (single source of truth) ──
LOGIN_EMAIL=demo@eyeheat.com
LOGIN_PASSWORD=demo123

# ── Frontend (Vite requires VITE_ prefix) ──
VITE_API_BASE_URL=http://localhost:3001
